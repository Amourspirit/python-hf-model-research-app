services:
  hf-exporter:
    build: .
    environment:
      - HF_TOKEN=${HF_TOKEN:-}
    volumes:
      - ./exports:/output
    stdin_open: true
    tty: true
    entrypoint: ["/bin/bash"]
    command: []

  hf-exporter-cli:
    build: .
    environment:
      - HF_TOKEN=${HF_TOKEN:-}
    volumes:
      - ./exports:/output
    command: ["--help"]

  hf-exporter-web:
    build: .
    environment:
      - HF_TOKEN=${HF_TOKEN:-}
    ports:
      - "8111:8000"
    volumes:
      - ./exports:/output
    entrypoint: ["uv", "run", "uvicorn"]
    command: ["hf_exporter.web:app", "--host", "0.0.0.0", "--port", "8000"]
